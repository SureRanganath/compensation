# ─── Database ─────────────────────────────────────────────────
# Neon PostgreSQL connection string
DATABASE_URL=postgresql://neondb_owner:npg_ITnHc4sX3pJW@ep-cold-poetry-aoo2v9wk-pooler.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
# ─── Authentication ──────────────────────────────────────────
# JWT secret key — Generate a strong random key:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=10f64bb8bd3a1a3314f2e367079bd9429ebb9692067e9d97b807ce78765f6dac

# JWT token expiry duration (default: 24h)
# Use '7d' for 7 days, '1h' for 1 hour, etc.
JWT_EXPIRES_IN=24h

# ─── Server ──────────────────────────────────────────────────
PORT=5000
NODE_ENV=development

# Comma-separated list of allowed CORS origins
ALLOWED_ORIGINS=http://localhost:3000
